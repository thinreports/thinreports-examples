# ImageBlock Tool - Thinreports Examples

## How to run

Bundle in this directory:

    % bundle install

Then execute `image_block.rb`:

    % bundle exec ruby image_block.rb

`result.pdf` will be created.

## Requirements

  * ruby >= 1.9
  * thinreports >= 0.7.7
